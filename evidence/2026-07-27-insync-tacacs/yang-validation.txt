SONiC YANG validation, run ON gpufab-s3-fabric switch dc1-pod001-bk-p1-r1-leaf01
(172.20.0.40), 2026-07-27. Read-only: sonic_yang.loadData + validate_data_tree in
a python process, never `config replace`, never applied.

This is the same check `config reload` and ZTP's configdb-json plugin run, so it
answers the one real risk of adding tables to the served artifact: could the new
artifact be REJECTED, disabling ZTP fleet-wide?

  tables_without_yang_model: []
  AUTH_TABLES_ALONE:  YANG-VALID
  FIXED_ARTIFACT:     YANG-VALID  tables=26 AAA=1 TACPLUS=1 TACPLUS_SERVER=1 PORT=64

FIXED_ARTIFACT is the config_db.json the CURRENT renderer produces for this
device (gwaddr present, auth tables on 172.20.0.3, SNMP dropped), rendered on the
workstation from the profile via model_sot and validated against the real image.

INCIDENTAL, and it is why the control failed first: the artifact s3 was actually
served, and the config the switch is RUNNING RIGHT NOW, are both YANG-INVALID —

  Must condition "(contains(current(), ':') and contains(../gwaddr, ':')) or
  (contains(current(), '.') and contains(../gwaddr, '.'))" not satisfied
  (path: .../MGMT_INTERFACE_LIST[name='eth0'][ip_prefix='172.20.0.40/24']/ip_prefix)

which is exactly the defect c80b58b fixed by adding gwaddr, independently
confirming that s3's artifacts predate it. Note that BOTH the unmodified artifact
and the auth-table version failed identically at first: a control that fails is
the instrument reporting on itself, not a verdict on the change.
